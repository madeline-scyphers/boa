# MultiObjective Optimization config
optimization_options:
    objective_options:
        objectives:
            - name: branin
              lower_is_better: True
            - name: currin
              lower_is_better: True
    trials: 10

parameters:
    x1:
        type: range
        bounds: [0, 1]
        value_type: float

    x2:
        type: range
        bounds: [0, 1]
        value_type: float

parameter_constraints:
    - x2 + x1 >= .1
    - x2 + x1 + .6*x1 <= .6
