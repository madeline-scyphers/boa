optimization:
    objective: some_metric
    metrics:
        some_metric:
            name: some_metric
            metric_type: passthrough

parameters:
    x1: 1.0

    x2: 2.0

    x3: 3.0

    x4: 4.0

    x5: 5.0


generation_strategy:
    method: custom
    initialization_budget: 10
    model_config:
        botorch_model_class: EnsembleMapSaasSingleTaskGP
        mll_class: ExactMarginalLogLikelihood
        name: BONSAI
    botorch_acqf_class: PosteriorMean

n_trials: 10
