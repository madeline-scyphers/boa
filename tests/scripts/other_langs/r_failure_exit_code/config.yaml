optimization:
    objective: metric
    metrics:
        metric:
            name: metric
            metric_type: passthrough
n_trials: 15

parameters:
    x0:
        'bounds': [ 0, 1 ]
        'type': 'range'
        'parameter_type': 'float'


script_options:
    run_model: Rscript run_model.R
    exp_name: "r_error_exit_code"
